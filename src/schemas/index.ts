import { courseSchema, createCourseSchema, readCourseSchema, updateCourseSchema } from "./course.schemas";
import { reqLoginSchema, resLoginSchema } from "./login.schemas";
import { userSchema, createUserSchema, returnUserSchema, readUserSchema, updateUserSchema } from "./users.schemas";
import { userCoursesSchema } from "./userCourse.schemas";

export {courseSchema, createCourseSchema, readCourseSchema, updateCourseSchema, reqLoginSchema, resLoginSchema, userSchema, createUserSchema, returnUserSchema, readUserSchema, updateUserSchema, userCoursesSchema};